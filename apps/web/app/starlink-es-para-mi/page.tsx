import { FoundationPage, generatePageMetadata } from "../page-template";

export const metadata = generatePageMetadata("/starlink-es-para-mi");

export default function StarlinkEsParaMiPage() {
  return <FoundationPage path="/starlink-es-para-mi" />;
}
